import React from "react";
import { FormattedMessage } from "react-intl";

const HomePage = () => (
  <div>
    <FormattedMessage id="home.welcome" /> front-studio-demo-r1!
  </div>
);

export default HomePage;
